# docs/

Your written decisions go here — the tickets name the files (`grain.md`, `scd.md`,
`reconciliation.md`, `idempotency.md`, `before.csv` / `after.csv`). A number in a doc should come
from a query committed under `analyses/`, so a reviewer regenerates it rather than trusting it.
