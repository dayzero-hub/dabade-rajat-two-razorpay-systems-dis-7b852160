version: 2

models:
  - name: fct_daily_orders
    description: Daily order counts and delivered revenue. This is the number that goes to finance.
    columns:
      - name: order_date
        tests: [unique, not_null]
