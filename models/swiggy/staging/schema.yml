version: 2

models:
  - name: stg_sw_orders
    columns:
      - name: order_id
        tests: [not_null]
      - name: order_date
        tests: [not_null]
      - name: amount_paise
        tests: [not_null]
